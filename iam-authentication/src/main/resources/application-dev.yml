server:
    port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jnvconnect_auth
    username: jnvconnect
    password: jnvconnect123
    driver-class-name: com.mysql.cj.jdbc.Driver

    # JPA Configuration
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: true

    # MongoDB Configuration
    data:
      mongodb:
        uri: mongodb://jnvconnect:jnvconnect123@localhost:27017/jnvconnect_tokens?authSource=admin
